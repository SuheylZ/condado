CREATE TABLE [dbo].[Overrides] (
    [ovr_id]           INT    NOT NULL,
    [ovr_account_id]   BIGINT NOT NULL,
    [ovr_ms_policy_id] BIGINT NOT NULL,
    [ovr_date]         DATE   NOT NULL
);

